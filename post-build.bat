@echo off
REM Implement post-build actions here, like calling a deploy script.
REM All commands must exit with code 0 on success. Anything else is considered failure.
REM %1 - the path to the build output directory